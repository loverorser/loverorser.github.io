# 关于GC

## 标记清除

- 标记位放对象头->写时复制不兼容->用位图->多个堆用多个位图
- 每次都要从头遍历找一个分块->寻找指针不每次重置，找完了放着不动
- 碎片问题->扫描的时候如果相邻则合并
- 每次都从头找一遍->根据要找的字节大小1 2 3给对应的指针，回收的时候同理
- BiBOP->

## 增量

第一阶段 遍历根节点 给到stack中

第二阶段 每次从stack取一点 标记上

第三阶段 每次取一点标记 清掉

