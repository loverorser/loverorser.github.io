# 关于启动时间优化

- 关闭ReloadDomain
  - 记得管理静态域等
- Json类提前转为生成代码
- 要http请求的网关和服务器地址 因为不变 所以直接拿缓存
- 缓存图和技能 要读json 用再读好了
- 关声音
- 代码提前添加好 标签 不每次遍历
- 加载场景按实际来进度



网络IO 序列化 消息头

文件IO AB包不压缩

CPU 分帧处理 削峰 预热PB MP 高频操作 预缓存 缓存命中

GPU UI合批 图集 场景 LOD 合批 MipMap 

内存 字符串 引用池