# 关于打包

- 先看水印 删掉manifest的folder

- 找到PatchManifest_MainPackage.bytes清单文件

- 找到版本号文件PatchManifest_MainPackage.version



- 更新版本号
- 下载PatchManifest_MainPackage.bytes

## 关于命令行打包

> "G:\Program Files\Unity\Hub\Editor\2022.3.62f2\Editor\Unity.exe" -quit -batchmode -buildTarget Win64 -projectPath "G:\UnityTest\TestScrollView" -logFile "E:\Build\Log.txt" -executeMethod Build.BuildExe

```
BuildPipeline.BuildPlayer(new string[1] { "Assets/Scenes/SampleScene.unity" }, "E:\\Build\\Build.exe", BuildTarget.StandaloneWindows64, BuildOptions.None);
```

可以在bat脚本定义好命令行参数，外面调用bat执行对应的参数，比如文件路径 unity路径，打包目标，额外的函数等。在unity里读取参数，进行对应的逻辑。

## 关于Jenkins

- 打包机上安装Jenkins
- 开端口 开账号
- 新建项目 定时 设置参数 执行bat