# 关于传东西

- 如果新增一个表，那么要传
  - 肯定加了某个txt表是吧，传txt表
  - generate一下，的bytes表，客户端和服务器都要传
  - 还有cs表，cs表是没有Category，xx.cs的。客户端和服务器都要传

- 如果新增一个协议，那么要传
  - 肯定动了OuterMessage.proto，要传
  - Generate一下，OuterMessage.cs的客户端和服务器都要传
  - 没了
- 如果新增一个数据库，那么要传***的有点多***
  - 先界面上改动，保存XML，三个XML要传
    - `Assets\Config\ParamPool\ParamXmlDefine.xml`
    - `Assets\Editor\ParamPool\ParamXmlDefineEditor.xml`
    - `ServerRes\Output\Develop\ServerOnlyConfig\ParamData\ParamXmlDefine.xml`
  - 再Generate一下